<header>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
          <%= link_to image_tag("open_box_logo.png"), reservations_path, class:"navbar-brand" %>
      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <%= selected?("foo") %>
            <li class=<%=selected?(reservations_path)%>> <%= link_to "Reservations", reservations_path %> </li>
          <% if signed_in? %>
            <li class=<%=selected?(edit_user_registration_path)%>> <%= link_to "Profile", edit_user_registration_path %> </li>
            <li> <%= link_to "Sign out", destroy_user_session_path, method: :delete%> </li>
          <% else %>
            <li> <%= link_to "Sign in", new_user_session_path %> </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
